#%RAML 1.0

title: Test API

/endpoint1:
  get:
    queryParameters:
      a:
        required: true
        type: string
    body: 
      application/json:
        properties:
          b:
            type: string
            maxLength: 20