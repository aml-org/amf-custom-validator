#%Validation Profile 1.0
profile: test

violation:
  - validation1

validations:

  validation1:
    targetClass: ex.Person
    propertyConstraints:
      ex.firstName:
        maxCount: 1
        datatype: xsd.string

prefixes:
  ex: http://datashapes.org/sh/tests/core/property/maxCount-001.test#

