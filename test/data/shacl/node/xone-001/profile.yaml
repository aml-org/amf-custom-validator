#%Validation Profile 1.0
profile: test

violation:
  - validation1

validations:

  validation1:
    targetClass: ex.Person
    xone:
      - propertyConstraints:
          ex.fullName:
            minCount: 1
      - propertyConstraints:
          ex.firstName:
            minCount: 1
          ex.lastName:
            minCount: 1

prefixes:
  ex: http://datashapes.org/sh/tests/core/node/xone-001.test#

